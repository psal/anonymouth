# Quick info for building with ant:

# First, cd to this directory (src/)

# Then, run whichever of these commands suits you:

# This will compile everything and launch the GUI
ant run-gui

# This will clean the build environment
ant clean